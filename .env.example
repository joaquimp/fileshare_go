# Configurações do File Share Server
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Porta do servidor (padrão: 8080)
PORT=8080

# URL base para geração de links de download (padrão: http://localhost:8080)
BASE_URL=http://localhost:8080

# Diretório onde os arquivos serão armazenados (padrão: ./uploads)
STORAGE_PATH=./uploads

# Tamanho máximo de arquivo em MB (padrão: 5)
MAX_FILE_SIZE_MB=5

# CONFIGURAÇÕES DE SEGURANÇA (OBRIGATÓRIAS)

# API Key para autenticação (OBRIGATÓRIA)
# Gere uma chave forte e única para seu aplicativo
# Exemplo: openssl rand -hex 32
API_KEY=sua_api_key_super_secreta_aqui_gere_uma_chave_forte

# User-Agent permitido (OPCIONAL - recomendado)
# Nome/identificador do seu aplicativo iOS
ALLOWED_USER_AGENT=ADAMA/1.0
